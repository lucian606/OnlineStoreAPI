spring:
  application:
    name: "OnlineStoreAPI"
  datasource:
    url: "jdbc:h2:mem:testdb"
    driverClassName: "org.h2.Driver"
    username: "sa"
    password: "password"
  jpa:
    database-platform: "org.hibernate.dialect.H2Dialect"

discount:
  # Discount percentage
  amount: 15

  # Strategy: expiration or category
  strategy: category

  # If expiration: expiration limit in days
  discounted-categories: meat
